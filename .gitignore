# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
node_modules/
web/dist/
web/playwright-report/
web/test-results/

# venvs
.venv/
venv/

# test / coverage
.pytest_cache/
.coverage
htmlcov/

# Large data extracts — never commit the full GB build (design §10)
data/*
# Curated, editable boat-hire enrichment input.
!data/boat-hire-enrichment.csv

# Build artifacts — serialized graph outputs (design §4.4)
artifacts/

# editor
.vscode/
.idea/

# Local map-prototype environment (may contain browser credentials)
.env.sh

# git worktrees (local isolation for feature work)
.worktrees/

# SDD per-run progress tracking (not project artifact)
progress.md

# Local Pi setup notes and routing data
docs/local/

# Local agent orchestration, review output, and helper scripts
.pi-subagents/
.pi/subagents/
reviews/
scripts/local/

# Disposable implementation plans
/docs/superpowers/plans/
