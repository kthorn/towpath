SEVERITY | SECTION | ISSUE | REQUIRED EDIT
The design introduces `PoiCandidate`, `PointOfInterest`, and `PoiIngestReport` models and specifies `WaterwayFeatures` gains `poi_candidates` and `poi_ingest_report`, but it does not define the `WaterwayFeatures` model or state where it is used. Function `load_artifact()` is said to return a `GraphArtifact` container, but the existing `save_artifact()` signature “save_artifact(graph, pois, path, metadata)” is retained, which is inconsistent because `load_artifact` returns a container but `save_artifact` does not accept one. | Define `WaterwayFeatures` as a model or clarify its location/role. Align `save_artifact()` signature to accept the same `GraphArtifact` container rather than separate `graph` and `pois` arguments, or else accept the container and validate it inside.

SEVERITY | SECTION | ISSUE | REQUIRED EDIT
The design states `GraphArtifact` is a frozen dataclass with `graph: nx.Graph`, `pois: tuple[PointOfInterest, ...]`, and `metadata: dict[str, object]`. However, section 4 also says the build-time writer `save_artifact(graph, pois, path, metadata)` constructs a `GraphArtifact` before serializing, and section 7 says the serialized pickle must contain exactly `Graph`, `pois`, and `metadata`. The structure of `GraphArtifact` is not final because the writer builds it but the payload keys are plain strings (`Graph`, `pois`, `metadata`), not dataclass attributes. | Decide whether `GraphArtifact` is the serialized form or a build-time container only. If it is the serialized form, the pickle keys must match the dataclass attribute names (lowercase `graph`, `pois`, `metadata`). If it is a build-time container, section 7 must specify the top-level pickle keys exactly.

SEVERITY | SECTION | ISSUE | REQUIRED EDIT
The design specifies that `nearest_coord_candidates()` uses an expanding search with a “conservative longitude/latitude envelope containing the complete spherical circle for radius `r`”. Section 6 also says “Pole-crossing envelopes cover every longitude; antimeridian-crossing envelopes are split into two STRtree boxes.” However, node STRtree uses WGS84 points in `(lon, lat)` order, and the envelope is built from the query coordinate plus radius, which can produce an ambiguous bounding box if the circle crosses the antimeridian or pole. The design does not specify the envelope calculation algorithm or how “conservative” is defined. | Specify the envelope calculation, e.g., using `haversine` to compute min/max lat and lon offsets, and describe how antimeridian and pole crossings are detected and split into multiple boxes before querying the STRtree.

NO_FINDINGS | | |
