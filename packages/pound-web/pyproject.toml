[project]
name = "pound-web"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "pound-core",
  "fastapi>=0.116,<0.142",
  "uvicorn[standard]>=0.35",
]

[tool.uv.sources]
pound-core = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/pound_web"]
